   #include <stdio.h>
   #include <stdint.h>

	void print_binary(uint8_t input)
	{
		uint8_t i = (1 << (sizeof (input)*8-1));
		for (; i; i >>= 1)
		{
			printf ("%d", (input&i)!=0);
		}
	
		printf ("\n");
	}

